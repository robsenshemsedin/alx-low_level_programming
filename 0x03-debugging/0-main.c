#include "main.h"

/**
 * main - debug wether a function prints correct out put when input is 0.
 *
 * Return: always return success.
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	_putchar('\n');
	return (0);
}
